{
  "name": "bookcreator-appengine-healthchecks",
  "version": "0.1.0",
  "repository": "bookcreator/bookcreator-appengine-healthchecks",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "eslint --cache --fix . && mocha",
    "preversion": "npm test",
    "postversion": "git push --tags"
  },
  "dependencies": {
    "@google-cloud/logging-winston": "^0.5.0",
    "winston": "^2.3.1"
  },
  "devDependencies": {
    "eslint": "^4.7.1",
    "mocha": "^3.5.3",
    "mockdate": "^2.0.2",
    "node-mocks-http": "^1.6.4"
  },
  "engines": {
    "node": "6.11.2"
  }
}
